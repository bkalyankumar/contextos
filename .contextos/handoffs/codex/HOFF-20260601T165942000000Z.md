---
handoff_id: HOFF-20260601T165942000000Z
task_id: TASK-006
from_agent: codex
to_agent: codex
status: marketing_split_to_private_repo
created_at: 2026-06-01T16:59:42+00:00
---

# Handoff: codex -> codex

## Current Task

Launch the ContextOS / Checkpoint repository and split marketing/cloud surfaces
out of the open-core repo.

## Current Status

In progress. The open-core `contextos` repo is public and released. The
marketing site files and Pages workflow have been moved to the private
`contextos-site` repo and pushed there. The open-core repo no longer contains
`site/` or `.github/workflows/pages.yml`, and GitHub Pages has been disabled on
the open-core repo.

## Summary

- Copied the marketing site from `/Users/bkakumar/contextos/site` into
  `/Users/bkakumar/contextos-site`.
- In `contextos-site`, placed the site at repo root:
  `index.html`, `README.md`, `CNAME`, and `.github/workflows/pages.yml`.
- Adjusted the Pages workflow in `contextos-site` to upload `.` and watch
  root-level site files.
- Committed and pushed `contextos-site` initial commit:
  `f6b62d7 feat: add ContextOS marketing site`.
- Verified `contextos-site` is private before attempting any deployment setup.
- Set `contextos-site` repo metadata to describe it as the private marketing,
  documentation, and cloud repo for ContextOS.
- Removed `site/` and `.github/workflows/pages.yml` from open-core `contextos`.
- Updated open-core launch docs, checklist, roadmap, and TASK-006 to reflect
  the repo split.
- Disabled GitHub Pages on open-core `contextos`; Pages API now returns 404.

## Files Changed

In `/Users/bkakumar/contextos-site`:

- `.github/workflows/pages.yml`
- `CNAME`
- `README.md`
- `index.html`

In `/Users/bkakumar/contextos`:

- deleted `.github/workflows/pages.yml`
- deleted `site/CNAME`
- deleted `site/README.md`
- deleted `site/index.html`
- `docs/launch.md`
- `docs/public-launch-checklist.md`
- `docs/roadmap.md`
- `.contextos/tasks/active/TASK-006-launch-repo.md`
- `.contextos/handoffs/latest.md`
- `.contextos/handoffs/codex/HOFF-20260601T165942000000Z.md`

## Tests Run

- In `contextos`: `.venv/bin/ruff check .` -> all checks passed.
- In `contextos`: `.venv/bin/mypy src` -> success.
- In `contextos`: `.venv/bin/pytest` -> 17 passed.
- In `contextos`: `.venv/bin/vulture src tests` -> no findings.
- In `contextos-site`: HTML parser structural check for `index.html` -> all
  in-page fragment links resolved.
- In `contextos-site`: expected launch-link scan -> GitHub, release, homepage,
  and CNAME references present.
- GitHub API: `bkalyankumar/contextos` remains public.
- GitHub API: `bkalyankumar/contextos-site` was verified private before Pages
  deployment setup was attempted.
- GitHub API: `repos/bkalyankumar/contextos/pages` -> 404 after disabling Pages.

## Blockers

Enabling Pages on private `contextos-site` was not performed. The safety reviewer
blocked it because publishing Pages from a private repo can disclose private
source content. Do it only after explicit approval that the private repo's built
site may be publicly deployed.

## Decisions

Keep `contextos` as the open-core CLI/schema/local-workflow repository. Keep
marketing, hosted docs, and future cloud code in private `contextos-site`.

## Next Recommended Agent

codex

## Continuation Prompt

Read AGENTS.md and `.contextos/handoffs/latest.md`. Continue TASK-006. The
marketing site has been moved to private `/Users/bkakumar/contextos-site` and
pushed as commit `f6b62d7`. The open-core `contextos` repo has `site/` and its
Pages workflow removed, and Pages has been disabled on the open-core repo.
Verify the final open-core cleanup commit is pushed. If the user explicitly
approves public deployment from private `contextos-site`, enable Pages there,
configure `contextos.quantumleapit.in`, and verify DNS/HTTPS.
